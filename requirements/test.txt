# Testing dependencies for pytest-based tests
pytest>=7.0.0
pytest-django>=4.5.0
Django>=4.2,<5.0
# These are also needed based on the imports in the models
numpy>=1.20.0
pandas>=1.3.0
python-dateutil>=2.8.0
pytz>=2021.1
six>=1.16.0
# For constance (dynamic settings)
django-constance>=2.9.0
# For celery tasks
celery>=5.2.0
# Additional Django packages that might be needed
whitenoise>=6.0.0

